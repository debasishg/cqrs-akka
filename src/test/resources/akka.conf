akka {
  event-handlers = ["akka.event.slf4j.Slf4jEventHandler"]
  event-handler-level = "DEBUG"
  actor {
    timeout = 20
    default-dispatcher {
      type = "Dispatcher"
      name = "trading-dispatcher"
      core-pool-size-factor = 20
      task-queue-size = 100
    }
  }
}
